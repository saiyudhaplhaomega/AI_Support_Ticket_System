const form=document.querySelector('#admin-chat'),result=document.querySelector('#admin-chat-result'),answer=document.querySelector('#admin-chat-answer');form.addEventListener('submit',async event=>{event.preventDefault();try{const response=await fetch('/api/admin/assistant',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({message:form.message.value})});const data=await response.json();if(!response.ok||!data.ok)throw new Error(data.detail||'Unable to answer.');document.querySelector('#admin-chat-text').textContent=data.answer;document.querySelector('#admin-chat-sources').replaceChildren(...data.sources.map(s=>{const li=document.createElement('li');li.textContent=typeof s==='string'?s:(s.source||s.title||'Knowledge source');return li;}));answer.hidden=false;result.textContent='';}catch(error){result.textContent=error.message;}});
